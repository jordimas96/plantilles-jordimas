# plantilles-jordimas

Initial angular repo setup for Angular 20:
* BaseHref /repoName/ only in production
* installed bootstrap and setup bootstrap custom
* installed fontAwesome 5.6.2
* general styles and utils classes:
    * reset
    * mediaqueries mixins
    * extra bootstrap spacing classes
    * bootstrap fixes

* buildPath: docs
* changed max production budget mb
* JMas favicon
